name: 05_Sequence_work_Flow
description: "This workflow runs before the completion of another workflow. Vide sample-5_1.yaml"

on:
  workflow_dispatch:

jobs:
  sleep_job:
    runs-on: ubuntu-latest
    steps:
      - name: sleep Step 1
        run: sleep 5